/// Fixture data backing the student Profile screen until the real backend lands.
library;

/// The signed-in student's email. The name / initial come from `mockUser`
/// (shared with Home); only the email is added here for the profile header.
const String mockProfileEmail = 'sarah.johnson@example.com';
