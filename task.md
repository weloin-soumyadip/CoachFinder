# CoachFinder — Task List

> Master checklist tracking work across all phases. Updated at the end of every step.

## Phase 1 — Project setup

- [x] **Step 0** — `flutter create` scaffolding (org `com.weloin`, project `coachfinder`, platforms `android,ios`)
- [x] **Step 1** — `pubspec.yaml` rewrite with the 9 runtime + 3 dev packages; Android `minSdk = 21`; iOS deployment target `12.0`; `flutter pub get`
- [x] **Step 2** — Skeleton folder + file creation (100 files across 80 directories, each `///` doc + `// TODO: implement`)
- [x] **Step 3** — Fully implement `core/` (theme, router, network, storage, error, providers) and `main.dart` (20 files, 14 expected screen-class errors blocking compile until Step 4)
- [ ] **Step 4** — Placeholder screens + real onboarding screen with role-selector logic

## Post-Phase-1 iterations

- [x] **Onboarding UI redesign** — select-then-continue flow with two role cards and Continue CTA (matches user's screenshot)
- [x] **Login screen UI** — branded badge, email + password with visibility toggle, Forgot Password link, Google / Apple buttons, Sign Up footer link. Submit / social / forgot-password buttons are stubbed via SnackBar (no backend wired)
- [x] **Register screen UI** — top bar with back arrow, Google / Apple, Full Name / Email / Password / Confirm Password, Terms checkbox with inline links, CREATE ACCOUNT button, Sign In footer link. Same stub pattern.
- [x] **`lib/shared/` folder added** — `shared/layouts/adaptive_navigation.dart` for the role-agnostic shell; `shared/widgets/` reserved. See [decision 0011](decisions/0011-shared-folder-and-adaptive-navigation.md).
- [x] **AdaptiveNavigation wired into both shells** — `_StudentShell` and `_OwnerShell` in `app_router.dart` now use `AdaptiveNavigation` (`NavigationBar` < 768 px, `NavigationRail` ≥ 768 px). Breakpoint bumped from 600 to 768 to keep phone landscape on the bottom bar. Green nav indicator added per Home design.
- [x] **Student Home (mobile)** — full layout built with fixture data: top bar, greeting, Next Session featured card, horizontally-scrolling Trending Topics rail, Recommended For You coach list, Personalized Path card with progress ring, action tiles row, chat FAB. Fixture file: `lib/features/student/home/data/mock_home_data.dart`. Widgets filled: `category_chip_widget.dart` (TopicChip) and `featured_card_widget.dart` (CoachCard).
- [x] **Teacher role added (3rd role)** — plumbing + placeholders. New `roleTeacher` constant, teal accent (`#0D9488`), third onboarding card, 5 teacher routes + `_TeacherShell` (Home · Search · Enquiries · Schedule · Profile), 5 placeholder screens under `lib/features/teacher/`. Redirect guard refactored from binary to N-role (`_homeFor` + generalized cross-shell check); `landingRouteForRole` added for the auth dev-shortcuts. Onboarding converted to scroll-safe layout so 3 cards don't overflow small phones. See [decision 0012](decisions/0012-teacher-role.md). Real teacher screen UIs are a later, per-design task.
- [x] **Debug test-credential sign-in bypass** — login now signs in only on `test@gmail.com` / `test-password` (`DevCredentials`), gated by `kDebugMode`; wrong input shows an error, release builds disable it. Register's dev-shortcut wrapped in the same `kDebugMode` gate so no backdoor ships. A debug-only hint pill under the Log In button shows the test credential. See [decision 0013](decisions/0013-debug-test-credential-bypass.md).
- [x] **Student Search (mobile + desktop)** — search bar with live local filtering, segmented tabs (All / Teachers / Institutes), resting state (browse-by-category chips + recent searches), and a "Found N …" results header with Filters link. Responsive grid (1 col phone, up to 3 on wide layouts, content capped at 1100px). New fixtures + models in `mock_search_data.dart` (`SearchTeacher`, `SearchInstitute`, `SearchEntityType`); widgets `search_field_widget.dart`, `teacher_result_card.dart`, `institute_result_card.dart`. Result taps + Filters are placeholders. See [decision 0014](decisions/0014-student-search-screen.md).
- [x] **Student Saved (mobile + desktop)** — search field + All / Coachings / Tutors filter over the student's bookmarked tutors and coaching institutes. Reuses the Search result cards (now with an optional `onUnsave` footer control) and the responsive grid; a working filled-bookmark un-save toggle removes items from local hook state and reveals an empty state. New dedicated `lib/features/student/saved/` feature (`mock_saved_data.dart` with `SavedFilter` enum + fixtures reusing the Search models; real `saved_screen.dart`). New shared `SavedBookmarkButton` in `lib/shared/widgets/`. The old placeholder under `profile/` and the stale `saved_center_tile_widget.dart` stub were removed; router import repointed. Un-save is local-only (no backend); card taps are placeholders. See [decision 0015](decisions/0015-student-saved-screen.md).
- [x] **Theme-aware colour migration (dark mode)** — introduced a `ThemeExtension<AppPalette>` (`lib/core/theme/app_palette.dart`) of brightness-aware semantic tokens (surface, background, text {primary,secondary,muted}, border(Subtle), iconFaint, inputFill, primary, primaryTint) accessed via a `context.palette` extension; registered on both themes with a per-brightness `scaffoldBackgroundColor`. **Light values are the original colours exactly** (light mode unchanged); dark = "Dim charcoal". Migrated 22 UI files off fixed `AppColors` neutrals/brand-foregrounds, keeping `studentPrimary` fills + white-on-colour foregrounds + fixed semantic tokens. Dark hexes added to `app_colors.dart`. Now the whole app (not just Material chrome) responds to the theme toggle. Foundation + 2 exemplars by hand, remaining ~18 files via 4 parallel subagents. See [decision 0017](decisions/0017-theme-aware-palette-migration.md).
- [x] **Tab bar visibility / theming** — `adaptive_navigation.dart` now colours the bottom `NavigationBar` + side `NavigationRail` entirely from the `ColorScheme`: transparent background (blends with the surface), no coloured indicator pill, selected = full-strength `onSurface` + bold + filled icon, unselected = `onSurfaceVariant` (dark in light theme, light in dark theme). Hover tooltips suppressed (`tooltip: ''`).
- [x] **Home cards bluish restyle** — gave the three Home sections a bluish, brand-consistent, theme-aware refresh: the Next Session ("Leadership Strategy") card now uses a `studentPrimary → studentPrimaryDark` gradient with a tighter heavier title; Trending-topic chips use `palette.primaryTint` (bluish, theme-aware) keeping the topic-coloured icon (lightened in dark); Recommended coach cards use a subtle bluish surface (`alphaBlend(studentPrimary@0.06, surface)`) + faint blue border, with the role line in `palette.primary` and a heavier/tighter name. Typeface unchanged (fixed system font) — "font style" = weight/spacing/colour. Supersedes the per-topic dark-mode chip tint from the earlier fix.
- [x] **Floating bottom-nav restyle** — reshaped `AdaptiveNavigation`'s compact bottom bar from a Material `NavigationBar` into a floating rounded `palette.surface` card (sp24 corners, side+bottom margins, soft `neutralBlack`@0.08 shadow), **icon-only** (active = `textPrimary` filled @26, inactive = `iconFaint` outlined @24, cross-faded via `AnimatedSwitcher`). Stays in the `bottomNavigationBar` slot so content isn't hidden; go_router shells, role tabs, and redirects are all unchanged. **Overlay (`extendBody`)** follow-up: the page now extends *behind* the floating bar (not a reserved slot); a shared width-aware `floatingNavClearance(context)` helper is applied as bottom scroll padding on all scrollable shell screens (+ lifts Home's chat FAB and the enquiry reply box) so nothing's hidden behind the bar. **Wide-screen rail** matched in a follow-up: the Material `NavigationRail` + divider became a `_FloatingSideRail` — a floating rounded `surface` card pill on the left, icon-only, sharing `_NavIcon` + the soft shadow (so both layouts have one floating-card aesthetic). Adapted from an external spec via `/flutter-ui` — declined its green palette / FAB / active-label / `lib/widgets`+`lib/screens` / `StatefulWidget`+`IndexedStack` / `print()` (would break the architecture + design system). Updated the nav test (icon-only assertions + supplies `AppTheme`); removed dead `_TopBar`/import in `search_screen.dart` to keep analyze clean. See [decision 0027](decisions/0027-floating-bottom-nav-restyle.md).
- [x] **Dark-mode fix: Home content-colour chips** — the Trending-topic chips (`category_chip_widget`) and coach tag pills (`featured_card_widget._TagPill`) painted fixed light pastels (fixture "content colours") as box fills, so they stayed bright in dark mode ("light boxes that don't darken"). Now in dark mode they tint the seed colour (`withValues(alpha: ~0.2)` → composites dark over the dark surface) and lighten the chip icon via HSL; light mode is pixel-identical. Documented the rule in the `flutter-ui` skill's fill-vs-foreground section. (Theme plumbing itself was already correct — `main.dart`/`app_theme.dart` wire light/dark + palette; no hardcoded light backgrounds existed.)
- [x] **Student Home animation + shared entrance widget** — added a staggered fade/slide **entrance** to the student Home feed (one `useAnimationController` + per-section intervals), an **animated progress ring** (`TweenAnimationBuilder` arc-fill + count-up on the Personalized-path card), and a wide-screen **width cap** (`Center` + `ConstrainedBox(720)`). Extracted the entrance helper into a reusable `lib/shared/widgets/entrance_fade_slide.dart` (`EntranceFadeSlide`) and refactored onboarding to use it (removed its private `_EntranceItem`). Built-ins only, no packages; content/behaviour unchanged. See [decision 0026](decisions/0026-student-home-animated-and-shared-entrance.md).
- [x] **Onboarding animated redesign** — rebuilt the role selector (same nav logic) with a brand hero lockup, three modern horizontal role rows each in its **own** accent (student blue / owner orange / teacher teal) with an animated radio/check indicator, and a Continue CTA that animates to the selected role's colour. Added a staggered fade-and-slide **entrance** via one `useAnimationController` + per-item `Interval` mapping (`_EntranceItem`), `AnimatedContainer`/`AnimatedScale` for selection — all built-in (no animation packages). Width-capped 480, bottom-pin preserved. See [decision 0025](decisions/0025-onboarding-animated-redesign.md).
- [x] **Auth redesign from spec — Sign In / Sign Up / Forgot Password** — adapted an external 3-screen spec to the project (blue brand, HookConsumerWidget + hooks, feature-folder widgets, named routes — *not* the spec's green/StatefulWidget/lib/widgets/raw-routes). Flat back-button + title + subtitle + form layout (replaces the [0023] hero). Form validation via `Form` + `useMemoized(GlobalKey<FormState>.new)` + new `AuthValidators` (notEmpty/email/password≥8/confirm-match) on `TextFormField`s; obscure toggles + "Remember for 30 days" via `useState`. New `ForgotPasswordScreen` + `/forgot-password` route (added to `isAuthRoute`). Upgraded `AuthFieldWidget` to a validating M3 floating-label field; removed unused `AuthHero`/`AuthHeroBackButton`/`AuthCard`/`AuthBrandingBadge` + register's terms checkbox. Debug login + role passing preserved (now gated by validate); social/Forgot are stubs. Fixed the spec's template bugs (Sign Up title, Forgot Password's stray password field, bottom links). See [decision 0024](decisions/0024-auth-redesign-from-spec.md).
- [x] **Auth screens modern redesign (hero + form)** — restyled login + register around a new shared `AuthHero` (full-bleed `studentPrimary → studentPrimaryDark` brand gradient with logo/wordmark/title/subtitle, rounded bottom, status-bar inset) with the form card floated over it via `Transform.translate`. Modernised `AuthFieldWidget` to a Material 3 **floating-label** filled field (Forgot-password link moved below the password field); added a border to `AuthCard`; new `AuthHeroBackButton`; removed the obsolete `AuthBrandingBadge` + `_RegisterTopBar`. **Visual refresh only** — debug test-credential login, role passing, stubs, terms checkbox, and all navigation unchanged. Added `studentPrimaryDark` token. First exercise of the `flutter-ui` skill. See [decision 0023](decisions/0023-auth-screens-modern-redesign.md).
- [x] **Teacher Profile (public listing read view + edit form)** — rewrote the teacher profile placeholder into an account profile + editable public listing (teal-branded). **Read view** (`teacher_profile_screen.dart`): identity card (avatar, name, headline, Independent/Affiliated **status badge**, rating, email), read-only views/students/rating/response **stats strip**, About / Subjects & Expertise / Rate & Experience sections, then the shared **Appearance** selector + **Settings** list + **Sign Out** tail (same themeModeProvider + sign-out flow as student/owner). **Edit form** (`edit_teacher_profile_screen.dart`, new route `/teacher-profile/edit` = `AppRoutes.teacherEditProfile`): name/headline/email/bio/expertise/rate/experience fields, inline teal subject multi-select, an Independent-tutor switch revealing an affiliation field, and Save (commit + snackbar + pop). Created the teacher's first data layer — `mock_teacher_profile_data.dart` (`TeacherProfile` + taxonomy + fixture) and a Riverpod `Notifier` (`teacher_profile_provider.dart`) so edits update the read view live. Edits in-memory only; sub-widgets inlined (teacher had no widget stubs). See [decision 0022](decisions/0022-teacher-profile-screen.md).
- [x] **Owner Center tab (manage-center: read view + edit form)** — built the Center tab as a **read view** (`manage_center_screen.dart`) of the coaching center — "My Center" + Edit button, read-only views/rating/reviews stats strip, identity card (logo/name/tagline/location), and About / Subjects / Boards / Class Timings / Photos / Contact / Courses & Fees sections — plus a separate **edit form** (`edit_center_screen.dart`, new route `/manage-center/edit` = `AppRoutes.ownerEditCenter`) with text fields, the four selector widgets, an editable fee list (add/remove), and a Save button (commit + snackbar + pop). Implemented all four scaffolded widget stubs (`subject_selector` / `board_selector` multi-select chips, `timing_editor` per-day switch + `showTimePicker` chips, `image_upload` placeholder gallery). New `mock_center_data.dart` (`CenterProfile`/`DayTiming`/`CenterPhoto`/`CourseFee` + taxonomy + `formatTimeOfDay`); implemented the `manage_center_provider.dart` stub as a Riverpod `Notifier` so edits update the read view live. Center identity/figures match the dashboard + owner profile. Edits in-memory only; photos are colour placeholders + "Add Photo" stub (no image picker in stack); `CreateCenterScreen` left as placeholder. Owner-branded, palette-first. See [decision 0021](decisions/0021-owner-manage-center-screen.md).
- [x] **Owner Enquiries (inbox + detail)** — built both the enquiry inbox list and the conversation/detail screen (were bare placeholders). **Inbox:** title + unread count, search (name/course/message), All·New·Replied·Archived filter pills, enquiry tiles (avatar, name, time, course tag, snippet, NEW badge + bold for unread), empty state. **Detail:** contact card (avatar, course chip, status chip, phone/email, Call/Email → "Coming soon"), chat-style message thread (student bubbles left, owner replies right in accent), pinned multi-line reply box (local echo → flips status to Replied), and app-bar Archive/Unarchive. First use of a Riverpod **`Notifier`** (`enquiriesProvider`, implemented the `enquiry_provider.dart` stub) so replies/status changes in detail update the inbox live across navigation. Implemented the `enquiry_tile_widget.dart` / `reply_input_widget.dart` stubs; new `mock_enquiry_data.dart` (`Enquiry`/`EnquiryMessage`/`EnquiryStatus`/`EnquiryFilter` + 5 seed enquiries). Seed ids `enq-101/102/103` match the dashboard preview so its tap-through resolves; tiles + dashboard preview now `pushNamed` the detail (back returns within the shell). Replies are in-memory only until backend. Owner-branded, palette-first. See [decision 0020](decisions/0020-owner-enquiries-screen.md).
- [x] **Owner Dashboard screen** — built the real `owner_dashboard_screen.dart` (was a bare placeholder): greeting header (owner first name + business name + notifications bell with unread dot), a 2×2 stat-card grid (Profile Views / New Enquiries / Avg Rating / Active Students with trend captions), a **hand-drawn 7-day Profile Views bar chart** (`views_chart_widget.dart` via `CustomPainter` — no charting package added), a 2×2 quick-actions grid (Edit Center → manage-center, Manage Courses → stub, View Enquiries → inbox, Share Link → stub), and a recent-enquiries preview list (tap → enquiry detail, View all → inbox). Implemented the `stat_card_widget.dart` / `quick_action_widget.dart` stubs; new `mock_dashboard_data.dart` fixtures (`DashboardStat`/`StatTrend`, `DailyViews`, `EnquiryPreview`) + dashboard strings; owner-branded with `AppColors.ownerAccent`, palette-first per [decision 0017]. Content capped at 720px. Backend controller/repo stubs left for later. See [decision 0019](decisions/0019-owner-dashboard-screen.md).
- [x] **Owner Profile tab + screen** — added a 4th tab (Profile) to the coaching-owner bottom navbar/rail and a real `owner_profile_screen.dart` mirroring the student profile: identity header (avatar + owner name + **business-name subtitle** + email + Edit Profile), Appearance System/Light/Dark selector (same `themeModeProvider` + Hive persistence as student), placeholder settings card (Notifications / Billing / Help & Support / About → "Coming soon"), and Sign Out behind a confirm dialog. Recoloured to the owner orange (`AppColors.ownerAccent`) for brand fills/accents; neutrals route through `context.palette`. New route `/owner-profile` (`AppRoutes.ownerProfile`, added to `_ownerPrefixes`); `_OwnerShell` grew 3 → 4 destinations. New `mock_owner_profile_data.dart` fixtures + `ownerProfileBilling` string. Edit/settings rows are placeholders. See [decision 0018](decisions/0018-owner-profile-tab.md).
- [x] **Student Profile + app-wide theme toggle** — real `student_profile_screen.dart` (fixture-backed, single column capped at 600px): identity header (avatar + name + email + Edit Profile), an **Appearance** System/Light/Dark selector, a placeholder settings list (Notifications / Payment Methods / Help & Support / About → "Coming soon" snackbar), and **Sign Out** behind a confirm dialog (clears token + role → `/onboarding`). Theme toggle is fully wired: new `themeModeProvider` + `keyThemeMode`, hydrated/persisted via `settingsBox` (mirrors role), `MaterialApp.themeMode` now watches it. New `mock_profile_data.dart` (email fixture). **Caveat:** existing screens use fixed `AppColors`, so Dark flips Material surfaces + persists but those bespoke screens don't visually darken yet — full theme-token migration is a follow-up. See [decision 0016](decisions/0016-student-profile-and-theme-toggle.md).
- [x] **Teacher Home (activity control center)** — replaced the teacher Home placeholder with a control-center mirroring the owner dashboard but teal-branded (`AppColors.teacherAccent`), neoglass: `BrandBackdrop` (teal orb) → capped 600px → greeting `GlassPanel` (first name + Independent/Affiliated status from `teacherProfileProvider` + bell stub), a 2×2 `NeoSurface` **stat grid** (Views / Students / Rating / Response, read live from the profile provider so home + profile never disagree), **Today's Sessions** `NeoSurface` rows (mode icon, subject, batch, time) + empty state, **Recent Enquiries** rows (avatar, name, wanted subject, "2h ago") with a teal "View all", and **quick actions** — filled-teal Edit Profile (`pushNamed(teacherEditProfile)`) + outlined Share Profile (stub). New fixture `mock_teacher_home_data.dart` (`TeacherSession`/`SessionMode`, `TeacherEnquiry` + 3 each) + teacher-home `AppStrings`. Owner's 7-day views chart deliberately skipped to keep it lean. Sub-widgets private to the screen; fixtures reset on restart. See [decision 0034](decisions/0034-teacher-home-screen.md).
- [x] **Teacher Search (find a center to affiliate with)** — replaced the teacher Search placeholder; reuses the student search structure (responsive 1–3 col grid capped 1100px, live case-insensitive filter) but teal-branded for the *distinct* purpose of discovering coaching centers to associate with (not the student's learn-from discovery). Teal gradient wash (`teacherAccentTint → background`); reuses the student `SearchFieldWidget` (cross-feature import, like Saved); a single **"Hiring now"** toggle pill (teal filled / glass) replacing the student All/Teachers/Institutes segments; **resting state** of browse-by-subject glass chips (reusing `teacherSubjectOptions`) + recent searches; **results** as a `Wrap` grid of new `CenterResultCard`s (`GlassPanel`: logo, name + rating, location, "● Hiring" badge, subject pills, full-width teal **Request to affiliate** `NeoButton`) + empty state. New fixture `mock_teacher_search_data.dart` (`AffiliationCenter` + 6 centers + recents) + teacher-search `AppStrings`. Card taps / affiliate request are "Coming soon" stubs; no separate Filters sheet. See [decision 0035](decisions/0035-teacher-search-screen.md).
- [x] **Teacher Enquiries (inbox + conversation + reply)** — built the teacher Enquiries tab as a teal-branded mirror of the owner enquiries feature (ADR 0020), but **flat** (not glass) per the user's earlier revert of the owner enquiry glass + the no-glass-in-`ListView` rule. New `lib/features/teacher/enquiries/`: `mock_teacher_enquiry_data.dart` (`TeacherEnquiry` + `TeacherEnquiryMessage{fromTeacher}` + `TeacherEnquiryStatus`/`TeacherEnquiryFilter`, 5 seeds — 2 new/2 replied/1 archived); `teacher_enquiry_provider.dart` (a Riverpod `Notifier` — `byId`/`addReply`/`archive`/`unarchive` — one source of truth so detail edits update the inbox live); **inbox** screen (title + unread count, search, All/New/Replied/Archived pills, `ListView` of tiles, empty state, capped 720px); **detail** screen (contact card with subject + status chips + Call/Email stubs, student-left/teacher-teal-right chat thread, app-bar Archive/Unarchive, pinned reply box); `teacher_enquiry_tile_widget.dart` + `teacher_reply_input_widget.dart`. New `AppRoutes.teacherEnquiryDetail` + nested `:id` GoRoute under `/teacher-enquiries`. Reused the generic `enquiries*`/`enquiry*` strings (no new strings). **Single-source reconciliation:** removed the home's duplicate lightweight `TeacherEnquiry`; the home "Recent Enquiries" preview now reads `mockTeacherEnquiries.take(3)` from this feature (`.ago`→`.timeAgo`), so home + inbox can't drift. In-memory only; Call/Email stubbed. See [decision 0036](decisions/0036-teacher-enquiries-screen.md).
- [x] **Teacher Schedule (week strip + a day's sessions)** — replaced the last teacher placeholder with a calendar-style schedule in the teacher neoglass style (chosen via design Q&A): teal `BrandBackdrop` → capped 600px → a horizontally-scrollable **week day-strip** (`_DayPill`: weekday + date + a "today" dot; selected = filled teal `Material`, rest = frosted `GlassPanel`, today's date tinted teal; selection in hook state defaulting to today) → a frosted `GlassPanel` **summary header** (calendar icon + full day label + "N session(s)") → **`NeoSurface` session rows** (start/end time column, hairline divider, subject + group, video/in-person mode icon) or an empty-state row. Session taps stubbed. New fixture `mock_teacher_schedule_data.dart` (`ScheduleSession`, `ScheduleDay` + a 7-day `mockScheduleWeek`, today = Wed 30 May) + teacher-schedule `AppStrings`. **Note:** defines its own `SessionMode` enum rather than reusing the home's — an `import`/`export` reuse attempt broke the const fixtures (`invalid_constant`), so a local enum is the pragmatic fix (no file imports both; flagged for a future hoist to `shared`). Completes the teacher shell's 4 primary tabs (Home/Search/Enquiries/Schedule). See [decision 0037](decisions/0037-teacher-schedule-screen.md).

## Known divergences

- **Android `minSdk`** — decision [0003](decisions/0003-min-sdk-android-21-ios-12.md) chose 21, but Flutter master's gradle plugin persistently rewrites `minSdk` → `flutter.minSdkVersion` (= 24) on every build. Effective shipping value is 24. Needs a follow-up: either find the override mechanism or update 0003.

## Future phases

- [ ] **Phase 2** — TBD (decided with user)

---

## Conventions

- Tick a box only when the step is fully done and verified.
- New decisions made during a step → add a new file under `decisions/` and link it from the relevant phase doc in `docs/`.
- New phase started → add `docs/phase-XX-<slug>.md` with the plan, and append a new section to this file.
